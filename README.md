🧾 QR Code Generator using Python

This is a simple Python project that generates QR codes for any text or URL you provide.
It uses the qrcode and Pillow libraries to create and save the QR code as an image (.png).

📸 Example Output
QR code generated!


This creates a file named example_qr.png in your project folder.

🧩 Features

Generate QR codes for:

URLs

Plain text

UPI Payment links

Customize:

QR size

Border width

Foreground and background colors

🧰 Requirements

Install dependencies:

pip install qrcode[pil]
